---
import { Building2 } from 'lucide-react'
import { features } from './data'
---

<section class="relative bg-white py-20 lg:py-32">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="grid items-start gap-16 lg:grid-cols-2">
      <div class="space-y-6">
        <div class="bg-primary mb-8 inline-flex items-center px-4 py-2">
          <span class="text-sm font-medium uppercase tracking-wide text-white">CARACTERÍSTICAS</span
          >
        </div>
        <h2
          class="font-heading text-primary mb-6 text-2xl font-bold leading-tight md:text-3xl lg:text-4xl"
        >
          Todo lo que necesitas para un despacho legal de vanguardia
        </h2>
        <p class="text-neutral text-base leading-relaxed">
          Desde la gestión de casos hasta el apoyo en cumplimiento, empoderamos a los equipos
          legales modernos para operar eficientemente, mantenerse a la vanguardia y entregar
          resultados excepcionales en un panorama legal dinámico.
        </p>
      </div>

      <div class="space-y-4">
        {
          features.map((feature) => (
            <div class="bg-primary flex items-start gap-4 p-6 text-white">
              <div class="flex-shrink-0">
                <Building2 className="w-6 h-6 text-white" />
              </div>
              <div class="space-y-2">
                <h3 class="text-lg font-semibold">{feature.title}</h3>
                <p class="text-sm leading-relaxed text-gray-300">{feature.description}</p>
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
